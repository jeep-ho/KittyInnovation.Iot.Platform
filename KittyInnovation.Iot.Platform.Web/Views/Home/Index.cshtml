@{
    ViewBag.Title = ViewBag.UserName;
}
<style type="text/css">
    body, html, #container {
        width: 100%;
        height: 800px;
        margin: 0 auto;
    }
</style>
<div id="container" tabindex="0"></div>
@section Scripts{
    @Scripts.Render("~/bundles/jqueryval")
    <script src="https://webapi.amap.com/js/marker2.js"></script>
    <script type="text/javascript" src="http://webapi.amap.com/maps?v=1.4.0&key=d851ffeb2503831e787637973ff1511e"></script>
    <script type="text/javascript">
        var map = new AMap.Map('container', {
            resizeEnable: true,
            zoom: 10,
            center: [116.480983, 40.0958]
        });
        var provinces = [200,100,150,100];
        var icon = new AMap.Icon({
            image: 'http://vdata.amap.com/icons/b18/1/2.png',//24px*24px
            //icon可缺省，缺省时为默认的蓝色水滴图标，
            size: new AMap.Size(24, 24)
        });
        var marker = new AMap.Marker({
            icon: icon,//24px*24px
            position: provinces[i].center.split(','),
            offset: new AMap.Pixel(-12, -12),
            map: map
        });
        $(function () {
            @*$.ajax({
                type:'GET',
                url: 'http://localhost:90/api/user',
                data: { },
                beforeSend: function (XHR) {
                    XHR.setRequestHeader('Authorization','@ViewBag.Ticket')
                },
                success: function (data, status) {
                    $('.jumbotron').html(data);
                    console.log(data);
                    console.log(status);
                },
                error: function (e) {
                    console.log(e);
                },
                complete: function () {
                    console.log('success')
                }
            });*@
        });
    </script>
}
